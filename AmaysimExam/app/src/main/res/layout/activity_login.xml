<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_login"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.panasiares.amaysimexam.view.activity.LoginActivity">

    <LinearLayout
        android:id="@+id/container_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:orientation="vertical">

        <EditText
            android:id="@+id/txt_email_add"
            style="@style/LoginEditTextStyle"
            android:layout_width="match_parent"
            android:drawableLeft="@mipmap/ic_mail"
            android:drawablePadding="10dp"
            android:hint="@string/txt_email_hint"
            android:inputType="textEmailAddress" />

        <EditText
            android:id="@+id/txt_password"
            style="@style/LoginEditTextStyle"
            android:layout_width="match_parent"
            android:drawableLeft="@mipmap/ic_lock"
            android:drawablePadding="10dp"
            android:hint="@string/txt_password_hint"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/btn_login"
            style="@style/ButtonStyle"
            android:layout_width="match_parent"
            android:layout_marginTop="15dp"
            android:text="@string/txt_login_lbl" />
    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/container_login"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="15dp"
        android:text="@string/txt_login_lbl"
        android:textColor="@color/white"
        android:textSize="@dimen/font_login_lbl"
        android:textStyle="bold" />

</RelativeLayout>
